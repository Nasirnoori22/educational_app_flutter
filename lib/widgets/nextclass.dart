import 'package:flutter/material.dart';

class NextClass extends StatelessWidget {
  const NextClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
